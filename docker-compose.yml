services:
  postgres:
    image: postgres:16
    container_name: clarix-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: clarix
      POSTGRES_USER: clarix
      POSTGRES_PASSWORD: clarix
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: clarix-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
